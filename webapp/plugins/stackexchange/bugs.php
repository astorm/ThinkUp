<?php
    return;        
        #LATER        
        var_dump("What's needed for a minimum viable project to share?");               
        var_dump("Add frontend UI -- What basic reports?");          
        var_dump("All the Dashbaord errors -- what goes in insights table");                                       
        var_dump("Remove all the random error_reporting/ini_set");
        var_dump("Logging for posts input");
        
        #NOT 1.0        
        var_dump("Change my 'network' columns to stackexchange_network to avoid complications?");
        var_dump("Setup foreign keys?");                            
        var_dump("Configuration Screen, etc. config.json? -- leave this here?");         
        var_dump("Gracefully handle too many API requiests");     
        var_dump("Add events in any plugin");
        var_dump("How to handle edits to posts?");
        var_dump("Should PulsestormStackexchangeCrawlSyncMysql brute force the answers/questions?");        
        var_dump("How to handle edits and updates? (if at all)");         